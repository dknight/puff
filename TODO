## general

- After node >= 19 use nodejs --watch flag instead of nodemon.
  In node 18.11+ --watch seems to be very unstable and buggy.

## current

- make simple js framework for interaction?
- automate insertion ofs custom props values into html docs