## general

- After node >= 19 use nodejs --watch flag instead of nodemon.
  In node 18.11+ --watch seems to be very unstable and buggy.

## current

X make list like element
X make great colors
X make web-component for docs
X write doc
X no lorem ipsum in docs, use only correct text!

- make simple js framework for interaction
- continue to work on colors
